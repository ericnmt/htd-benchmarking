[project]
name = "htd-benchmarking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Eric Johnson", email = "eric.johnson@student.nmt.edu" }
]
requires-python = ">=3.9"
dependencies = [
    "matplotlib>=3.9.4",
    "numpy>=2.0.2",
    "pyod>=3.6.5",
    "scikit-learn>=1.6.1",
]

[project.scripts]
htd-benchmarking = "htd_benchmarking:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
